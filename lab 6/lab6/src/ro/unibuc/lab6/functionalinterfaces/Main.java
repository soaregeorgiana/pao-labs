package ro.unibuc.lab6.functionalinterfaces;

/**
 * Java functional interface can be implemented by a Java Lambda Expression
 * A Java lambda expression implements a single method from a Java interface.
 * In order to know what method the lambda expression implements,
 * the interface can only contain a single unimplemented method => the interface must be a Java functional interface
 */
public class Main {

    public static void main(String[] args) {

    }
}
