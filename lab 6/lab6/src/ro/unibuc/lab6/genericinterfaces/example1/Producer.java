package ro.unibuc.lab6.genericinterfaces.example1;

/**
 * A generic Java interface is an interface which can be typed
 * (it can be specialized to work with a specific type when used)
 */
public interface Producer {

    Object produce();
}
